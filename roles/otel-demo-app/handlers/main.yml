- name: start app agent
  command: docker compose up -d
  args:
    chdir: "{{ otel_demo_app_base_dir }}"

- name: restart app agent
  command: docker compose restart
  args:
    chdir: "{{ otel_demo_app_base_dir }}"
