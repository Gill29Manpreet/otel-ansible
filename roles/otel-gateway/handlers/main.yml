- name: start otel gateway
  command: docker compose up -d
  args:
    chdir: "{{ otel_base_dir }}/docker-compose"

- name: restart otel gateway
  command: docker compose restart
  args:
    chdir: "{{ otel_base_dir }}/docker-compose"
